# english-ai-cdn


For a given scenario ID, to get the image(s) for it the URL is:

`<scenario_id>`: The scenario ID
`<image_name>`: The image name, e.g. `bg` for background

```
https://raw.githubusercontent.com/colourdelete/english-ai-cdn/main/<scenario_id>-<image_name>.png
```
